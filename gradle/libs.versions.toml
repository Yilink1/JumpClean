# Project version catalog
# Note: Currently dependencies and plugins are managed directly in build.gradle.
# Template entries (junit, espresso, appcompat, material, etc.) have been cleaned up as they were unused.

[versions]

[libraries]

[plugins]
